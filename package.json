{
  "name": "simple-test-observable-store",
  "version": "0.0.2",
  "description": "simple observable store",
  "repository": "https://github.com/crossee/simple-obs-store.git",
  "author": "crossee",
  "license": "MIT",
  "main": "./dist/index.js",
  "scripts": {
    "setup": "yarn install",
    "prepublishOnly": "yarn build",
    "build": "rm -rf dist && tsc --project ."
  },
  "dependencies": {
    "@metamask/safe-event-emitter": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "typescript": "^4.0.5"
  }
}
